{
  "_comment": "Add this snippet to your Cursor config: .cursor/mcp.json in project root (project-level) or ~/.cursor/mcp.json (global)",
  "_instructions": [
    "Replace /ABSOLUTE/PATH/TO/ with the actual path to your cloned purdue-brightspace-mcp repo",
    "The 'env' object is intentionally empty — no credentials belong in this file",
    "Run 'purdue-brightspace-auth' first to authenticate before using with Cursor",
    "After adding this config, restart Cursor to load the MCP server"
  ],
  "mcpServers": {
    "purdue-brightspace": {
      "command": "node",
      "args": ["/ABSOLUTE/PATH/TO/purdue-brightspace-mcp/build/index.js"],
      "env": {}
    }
  }
}
